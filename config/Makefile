PREFIX=TARGET_PREFIX
BINDIR=TARGET_BINDIR/HOST_ARCH
LIBDIR=$(PREFIX)/lib/ocaml
STUBLIBDIR=$(LIBDIR)/stublibs
MANDIR=OCAML_SRC/no-man
MANEXT=1
SYSROOT=/
RANLIB=TARGET_PATH/HOST_ARCH-ranlib
RANLIBCMD=TARGET_PATH/HOST_ARCH-ranlib
ARCMD=TARGET_PATH/HOST_ARCH-ar
SHARPBANGSCRIPTS=true
BNG_ARCH=generic
BNG_ASM_LEVEL=0
PTHREAD_LINK=
X11_INCLUDES=not found
X11_LINK=not found
LIBBFD_LINK=TARGET_LDFLAGS
BYTECC=TARGET_PATH/HOST_ARCH-gcc --sysroot $(SYSROOT) -I $(PREFIX)/include -L $(PREFIX)/lib -O2
BYTECCCOMPOPTS=-fno-defer-pop -Wall -D_FILE_OFFSET_BITS=64 TARGET_CFLAGS
BYTECCLINKOPTS=-Wl,-E TARGET_LDFLAGS
BYTECCLIBS=TARGET_MATHLIB -ldl
BYTECCRPATH=-Wl,-rpath,
EXE=
SUPPORTS_SHARED_LIBRARIES=true
SHAREDCCCOMPOPTS=-fPIC
MKSHAREDLIBRPATH=-Wl,-rpath,
NATDYNLINKOPTS=-Wl,-E
SYSLIB=-l$(1)
#ml let syslib x = "-l"^x;;

### How to build a static library
MKLIB=TARGET_PATH/HOST_ARCH-ar rc $(1) $(2); TARGET_PATH/HOST_ARCH-ranlib $(1)
#ml let mklib out files opts = Printf.sprintf "TARGET_PATH/HOST_ARCH-ar rc %s %s %s; TARGET_PATH/HOST_ARCH-ranlib %s" out opts files out;;
ARCH=TARGET_OCAML_ARCH
MODEL=TARGET_MODEL
SYSTEM=TARGET_SYSTEM
NATIVECC=$(BYTECC)
NATIVECCCOMPOPTS=-Wall -D_FILE_OFFSET_BITS=64 TARGET_CFLAGS
NATIVECCPROFOPTS=$(NATIVECCCOMPOPTS)
NATIVECCLINKOPTS=TARGET_LDFLAGS
NATIVECCRPATH=-Wl,-rpath,
NATIVECCLIBS=TARGET_MATHLIB -ldl
ASM=TARGET_PATH/HOST_ARCH-as
ASPP=$(BYTECC) -c
ASPPPROFFLAGS=-DPROFILING
PROFILING=prof
DYNLINKOPTS= -ldl
OTHERLIBRARIES=unix str num dynlink bigarray threads
CC_PROFILE=-pg
SYSTHREAD_SUPPORT=false
PARTIALLD=TARGET_PATH/HOST_ARCH-ld -r
PACKLD=$(PARTIALLD) $(NATIVECCLINKOPTS) -o\
DLLCCCOMPOPTS=
IFLEXDIR=
O=o
A=a
SO=so
EXT_OBJ=.o
EXT_ASM=.s
EXT_LIB=.a
EXT_DLL=.so
EXTRALIBS=
CCOMPTYPE=cc
TOOLCHAIN=cc
NATDYNLINK=false
CMXS=cmxa
MKEXE=$(BYTECC) TARGET_LDFLAGS
MKEXEDEBUGFLAG=-g
MKDLL=$(BYTECC) -shared TARGET_LDFLAGS
MKMAINDLL=$(MKDLL)
RUNTIMED=noruntimed
ASM_CFI_SUPPORTED=false
WITH_FRAME_POINTERS=false
# New to OCaml 4.02
UNIX_OR_WIN32=unix
UNIXLIB=unix
GRAPHLIB=
WITH_DEBUGGER=
WITH_OCAMLDOC=
WITH_OCAMLBUILD=
TARGET=HOST_ARCH
HOST=HOST_ARCH
# No longer used in OCaml 4.02
TK_DEFS=
TK_LINK=
DEBUGGER=ocamldebugger
CAMLP4=
